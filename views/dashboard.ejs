<form method="GET" action="/sql">
    <input type="text" placeholder="category" name="category">
    <input type="number" placeholder="amount" name="amount" step="0.01">
    <input type="text" placeholder="note" name="note">
    <input type="submit">
</form>

<button id="get">Show expenses</button>

<table id="expenses">
    <thead>

    </thead>
    <tbody>

    </tbody>
</table>

<div style='width: 350px; height: 400px; display: block;'> <!-- v tomto div-e je pie chart, velkost pieChartu nastavovat cez tento div -->
<div> <!-- v tomto div-e je pie chart -->
    <canvas id='pieChart'>
    </canvas>
</div>

<script>
    function get() {
        
        let table = $("#expenses tbody").html("");
        $.ajax({
            url: "/sql",
            contentType: "application/json",
            success: function (res) {
                console.log(res)
                res.forEach(function(item){
                    table.append(`\
                    <tr>\
                        <td>${item["date"]}</td>\
                        <td>${item["spent"]}</td>\
                        <td>${item["category"]}</td>\
                        <td>${item["note"]}</td>\
                    </tr>\
                    `)
                });
            }
        });
    }

    function post() {
        console.log("post");
    }

    $(function(){
        var ctx = document.getElementById('pieChart');
        var myChart = new Chart(ctx, {
            type: 'pie',
            data: {
                labels: ['zostato', 'minute'],
                datasets: [{
                    label: 'peniaze',
                    data: [1,1],
                    backgroundColor: [
                        'rgba(255, 99, 132, 0.2)',
                        'rgba(54, 162, 235, 0.2)',
                    ],
                    borderColor: [
                        'rgba(255, 99, 132, 1)',
                        'rgba(54, 162, 235, 1)',
                    ],
                    borderWidth: 1
                }]
            },
            options: {
                responsive:true,
                maintainAspectRatio: false,

            }
        });



        $("#get").on("click", function () {
            get();
        })
    });
</script>
</body>